# Lives in taskpilot because a Gateway listener can only reference a Secret in its
# own namespace. Start on staging, confirm Ready, then switch to prod - in GIT,
# since selfHeal reverts kubectl patch.


# This code requires domain

# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: taskpilot-tls
#   namespace: taskpilot
# spec:
#   secretName: taskpilot-tls
#   dnsNames:
#     - taskpilot.trainwithshubham.com
#   issuerRef:
#     name: letsencrypt-staging
#     kind: ClusterIssuer
#     group: cert-manager.io
#   privateKey:
#     algorithm: ECDSA
#     size: 256
#     rotationPolicy: Always
#   # Let's Encrypt certs last 90 days; renew 30 days out.
#   renewBefore: 720h