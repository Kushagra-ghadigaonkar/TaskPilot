apiVersion: v1
kind: Secret
metadata:
  name: dev-secret
  namespace: taskpilot
type: Opaque
stringData:
  POSTGRES_USER: "TaskPilot"
  POSTGRES_PASSWORD: "TaskPilot"
  POSTGRES_DB: "TaskPilot"
  POSTGRES_URL: "postgres://TaskPilot:TaskPilot@postgres:5432/TaskPilot?sslmode=disable"
  BACKEND_PORT: "8080"
  POSTGRES_HOST_PORT: "5432"
  BACKEND_HOST_PORT: "5000"
  FRONTEND_HOST_PORT: "8080"