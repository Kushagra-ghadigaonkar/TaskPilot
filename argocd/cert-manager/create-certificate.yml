apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: taskpilot-tls
  namespace: taskpilot
spec:
  secretName: taskpilot-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - taskpilot.local